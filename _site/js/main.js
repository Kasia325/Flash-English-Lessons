// Load the DOM
$(document).ready(function(){


  //Log
  console.log('Ready!');

    // Click the hamburger icon
    $('.toggle-mobile-menu').click(function(){

        // Show the mobile menu
        $('#mobile-menu').toggleClass('mobile-menu-visible');

    });

    // Show the mobile menu

    // Click the cross item

    // Close the mobile menu


});
